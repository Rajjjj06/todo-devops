

services:
  frontend:
    build: ./frontend
    ports: 
      - "3000:80"
    env_file: 
      - ./frontend/.env 
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env